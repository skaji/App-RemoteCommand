prereqs:
  configure:
    requires:
      Dist::Build: { version: '0.015' }
  runtime:
    requires:
      Getopt::Long: { version: '2.39' }
      IO::Pty:
      List::Util: { version: '1.45' }
      Net::OpenSSH:
      String::Glob::Permute:
      Term::ReadKey:
      perl: { version: 'v5.16' }
  develop:
    requires:
      Capture::Tiny:
      File::pushd:
